{
  "name": "gezi",
  "version": "0.1.0",
  "private": true,
  "description": "GeoPixel Wall — 公共像素墙（Cloudflare Workers + D1 + R2）",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:css": "tailwindcss -i ./styles/app.css -o ./public/assets/app.css --minify",
    "watch:css": "tailwindcss -i ./styles/app.css -o ./public/assets/app.css --watch",
    "dev": "npm run build:css && wrangler dev",
    "deploy": "npm run build:css && wrangler deploy",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "db:migrate:local": "wrangler d1 migrations apply gezi --local",
    "db:migrate:remote": "wrangler d1 migrations apply gezi --remote"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260908.1",
    "@playwright/test": "^1.54.0",
    "@tailwindcss/cli": "^4.3.3",
    "@types/node": "^24.13.3",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.8.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.130.0"
  }
}
